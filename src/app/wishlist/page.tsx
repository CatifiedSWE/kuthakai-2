import { WishlistScreen } from '@/modules/wishlist';

export const metadata = {
  title: 'My Wishlist | Kuthakai',
  description: 'View and manage your saved items',
};

export default function WishlistPage() {
  return <WishlistScreen />;
}
